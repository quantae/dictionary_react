import { ThemeContext } from "../../services/context/themeContext";


export const  darkThemeStyle =  {
    background: {
      background: isDark => isDark && "#dedede",
    },
  };

  //export default darkThemeStyle;